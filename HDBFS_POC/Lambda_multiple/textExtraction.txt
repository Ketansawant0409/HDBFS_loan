import json
import boto3

def lambda_handler(event, context):
    dynamodb_client = boto3.client('dynamodb',region_name="us-east-1",
                             aws_access_key_id="AKIAS75ADNXVKGS4WMKB",
                             aws_secret_access_key="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")

    table_name = 'newtable'
    item = {
        'employee_name': {'S': 'Ketan'},
        'lastname': {'S': 'Sawant'} } 
    try:
        response = dynamodb_client.put_item(TableName=table_name, Item=item)
        return "Item inserted successfully!"
    except Exception as e:
        return f"Error inserting item: {e}"